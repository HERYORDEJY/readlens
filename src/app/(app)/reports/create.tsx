import { Text, View } from "react-native";

import { Screen } from "@/components/screen";

export default function CreateReportScreen() {
    return (
        <Screen>
            <View>
                <Text>Create report screen.</Text>
            </View>
        </Screen>
    );
}
